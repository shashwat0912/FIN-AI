# Archived SQLite migrations

These migrations belong to Finance AI's original SQLite implementation. They
are retained only as migration and audit history and are not part of the active
Prisma migration chain.

The application now uses PostgreSQL. Its active baseline and forward migrations
live in `../migrations/`, with reviewed baseline evidence in `../review/`.
